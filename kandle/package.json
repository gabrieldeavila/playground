{
  "name": "kandle",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,ui \"pnpm --dir backend run start:dev\" \"pnpm --dir ui run dev\"",
    "build": "pnpm --dir backend run build && pnpm --dir ui run build",
    "start": "concurrently -k -n backend,ui \"pnpm --dir backend run start:prod\" \"pnpm --dir frontend run start\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}